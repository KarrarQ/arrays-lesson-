var cars = ["Pen", "Pencil", "Book,"];
console.log(cars.unshift(1))
//the unshift adds 1 or more elements to the begining of an array
var lamps = [1, 2, 3, 4, 5, 6];
console.log(lamps.slice(0, 2));
//slice does not change the orignal array. It returns a differnt copy of elements from the original
var Books = ["true", "flase", "yes", "no"]
books.pop();
console.log(books);
var books ="true"
console.log(books);
//pop method returns last element and take it off.

//Demonstrate your understanding of index positions in this file.
/*
An index is reference to a position in a function;
The order of the position.
It can return the value of the specified attribute for the current position,
or the position with the supplied index number 8*/
